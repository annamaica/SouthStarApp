<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.maica.southstarapp">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="South Star Drug"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme1">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Reminder" />
        <activity android:name=".Dashboard" />
        <activity android:name=".View_Product" />
        <activity android:name=".Add_Product" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".CreateAccount" />
        <activity android:name=".View_medicine" />
        <activity android:name=".EditMedicine" />
        <activity android:name=".CreateAccountInfo" />
        <activity android:name=".User_Dashboard" />
        <activity android:name=".User_View_Product" />
        <activity android:name=".User_View_medicine" />
        <activity android:name=".Order_NonPrescription" />
        <activity android:name=".View_Order" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View_cart" />
        <activity android:name=".Transaction_history" />
        <activity android:name=".Prescription" />
        <activity android:name=".Order_Prescription" />
        <activity android:name=".User_View_Order" />
        <activity android:name=".User_Order_List" />
        <activity android:name=".Cart_Edit" />
        <activity android:name=".Order_transac_prescription" />
        <activity android:name=".SalesModule" />
        <activity android:name=".Prescription_cart" />
        <activity android:name=".Prescription_cart_edit" />

        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true"
            android:exported="true" />

        <service
            android:name=".RingtonePlayingService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".Medicine_dictionary" />
        <activity android:name=".Medicine_dictionary_view" />
        <activity android:name=".Chat" />
        <activity android:name=".Chat2" />
        <activity android:name=".Chat_Message"></activity>
    </application>

</manifest>